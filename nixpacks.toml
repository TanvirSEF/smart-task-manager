# nixpacks.toml for Next.js deployment on Dokploy

[variables]
NODE_VERSION = "20"
NIXPACKS_NODE_VERSION = "20"

[phases.install]
dependsOn = ["setup"]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
dependsOn = ["install"]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
